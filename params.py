from preferences import Calibre

params = {
    "MaxTime" : "21:00",
    "MaxDuration" : 45,
    "Calibres" : [Calibre.BPLUS, Calibre.A, Calibre.AA]
}

# print(params.keys())
# print(params.values())
# print(params.items())
# print(params["MaxTime"])
